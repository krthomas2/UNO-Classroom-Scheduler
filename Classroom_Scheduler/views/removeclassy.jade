extends layout
block content
   h1 Calendar
   p.right.col-md-3 Select Room:
      select.form-control
         #rooms
            -each room in rooms
               option #{room.Subject+ " " +room.Course_ID + " ID: " +room.Class_ID}

